modules:

  - type: rpm-ostree
    repos:
      - netbird.repo # Copies in .repo file from files/rpm-ostree/. to /etc/yum.repos.d/.
    install:
      - netbird
      - netbird-ui

  - type: systemd
    system:
      enabled:
        - netbird
    user:
      enabled:
        - netbird-ui
