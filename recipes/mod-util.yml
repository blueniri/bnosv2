modules:

  - type: rpm-ostree
    install:
      - fontconfig           # Font management
      - mediawriter          # Fedora Media Writer
      - keyd

  - type: dnf
    repos:
      copr:
        - alternateved/keyd
