modules:

  - type: default-flatpaks
    configurations:
      - scope: user
        install:
          # Browser
          - org.mozilla.firefox
          - org.gnome.Loupe

          # Chat
          - org.gajim.Gajim
          - im.dino.Dino

          # IDE/Text Editing
          - org.geany.Geany
          - org.kde.kate
          - org.kde.kwrite
